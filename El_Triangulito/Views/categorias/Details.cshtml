@model El_Triangulito.Models.categorias

@{
    ViewData["Title"] = "Details";
}

<div class="container mt-5">
    <h1 class="text-white">Details</h1>

    <h4 class="text-white">categorias</h4>
    <hr class="bg-light" />
    <div class="card bg-dark text-white">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.categoria)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.categoria)
                </dd>
            </dl>
        </div>
    </div>
    <div class="mt-3">
        <a asp-action="Edit" asp-route-id="@Model?.id_categoria" class="btn btn-warning">Edit</a>
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </div>
</div>